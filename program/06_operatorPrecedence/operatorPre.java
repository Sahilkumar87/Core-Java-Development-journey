import java.util.*;
public class operatorPre {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);

     /*
      -> operator precedence: evaluation order of operator.
      ->associativeity: order of operatin for operator with thhe same precedence.
      */
        
    }
}